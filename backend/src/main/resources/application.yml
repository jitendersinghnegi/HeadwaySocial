server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8082
 # servlet:
    #context-path: /api

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: headwaysocial
    password: headwaysocial
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: trues



